// IMPORT MONGOOSE MODELS.

/* METHODS */
/* 1. MONGO DB: */
const getPostDeleteSomething = async (id) => {
    const response = "Requests to the DAC-Portal-DB"

    return response
}

exports.getPostDeleteSomething = getPostDeleteSomething;
